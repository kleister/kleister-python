[tox]
envlist = py27, py3, flake8

[testenv]
commands = {envbindir}/nosetests --with-coverage --cover-package kleister -v
deps = -r{toxinidir}/dev-requirements.txt

[testenv:flake8]
commands = flake8 setup.py kleister tests
deps = flake8
